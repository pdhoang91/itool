/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [], // Thêm domains nếu cần
      remotePatterns: [], // Thêm remote patterns nếu cần
    },
  }
  
  module.exports = nextConfig